A sample command-line application with an entrypoint in `bin/`, library code
in `lib/`, and example unit test in `test/`.


Stu(Grade[] grades, bool inter, bool full, bool prob) inherits Person

Grade(Cours class, int score)

Prof(Cours[] taught, int salary, bool bonus) inherits Person

Person(String addy, String phone, String name, int age)

Cours(String name, Prof[] profs, Stu[] stus, int max, int min, bool active)